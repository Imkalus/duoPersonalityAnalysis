// ============================================================================
// 前端运行时配置
// ----------------------------------------------------------------------------
// 这里集中存放可调开关。可直接改这里的默认值，
// 或在项目根目录 .env 中设置对应的 VITE_ 变量（构建时注入，优先级更高）。
// ============================================================================

// 是否显示「一键填充（跳到最后一题）」调试按钮。
// 生产环境通常关闭：在 .env 设置 VITE_SHOW_FILL_ALL=false 即可隐藏。
export const SHOW_FILL_ALL_BUTTON =
  import.meta.env.VITE_SHOW_FILL_ALL === undefined
    ? true
    : import.meta.env.VITE_SHOW_FILL_ALL === 'true';
